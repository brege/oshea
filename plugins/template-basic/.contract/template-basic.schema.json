{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "template-basic.schema.json",
  "title": "Schema for md-to-pdf template-basic plugin",
  "description": "This schema extends the base plugin schema with properties specific to the 'template-basic' plugin.",
  "type": "object",
  "allOf": [
    {
      "$ref": "base-plugin.schema.json"
    }
  ],
  "properties": {
    "params": {
      "type": "object",
      "properties": {
        "default_greeting": {
          "description": "A default greeting placeholder for the example document.",
          "type": "string"
        }
      },
      "additionalProperties": true
    }
  }
}
