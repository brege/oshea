path.join(manager.collRoot
path.resolve(source)
os.homedir()
manager.collRoot
pluginDirectoryPath
process.env
require(fullPath)
require(tocPlugin)
require(pluginConfig)
require(pluginName)
path.extname(dirent.name)
path.join(pluginDirectoryPath, '.contract')
console.
path.basename(
path.dirname(
path.isAbsolute(
.collection-metadata.yaml
path.join(archetypePath, '.contract');
manager.collRoot, args.target, '.collection-metadata.yaml
