# test/assets/override_config/cv_test.yaml
# This is the file passed to the --config CLI flag for the test.

document_type_plugins:
  cv: "./override_cv.config.yaml" # Path relative to the cv_test.yaml file

# We can also test overriding a global option here if desired,
# e.g., to ensure it's picked up by ConfigResolver._loadPrimaryMainConfig()
# For this test, we'll focus on the plugin override.
# global_pdf_options:
#   printBackground: false # Example global override for this project config
