{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "valid-plugin.schema.json",
  "title": "Schema for oshea valid-plugin plugin",
  "description": "This schema extends the base plugin schema with properties specific to the 'valid-plugin' plugin.",
  "type": "object",
  "allOf": [
    {
      "$ref": "base-plugin.schema.json"
    }
  ],
  "properties": {
    "params": {
      "type": "object",
      "properties": {
        "default_greeting": {
          "description": "A default greeting placeholder for the example document.",
          "type": "string"
        }
      },
      "additionalProperties": true
    }
  }
}
