name: Validate bundled plugins
test_id: P.3.3.bundled
base_command: md-to-pdf
tags:
  - cli
  - smoke
  - plugin
  - bundled
  - validate
scenarios:
  - description: Validate plugin {item}
    test_id: P.3.3.{item}
    args: plugin validate {item}
    expect:
      executes: true
discovery:
  type: directory_scan
  source: plugins
  filter: directories
