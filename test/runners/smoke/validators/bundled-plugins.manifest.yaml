# Smoke test manifest - all CLI smoke tests
name: "P.3.3 Validate bundled plugins"
base_command: "md-to-pdf"
tags: [cli, smoke, plugin, bundled, validate]
discovery:
  type: "directory_scan"
  source: "plugins"
  filter: "directories"
scenarios:
  - description: "P.3.3.1 Validate plugin {item}"
    args: "plugin validate {item}"
    expect:
      executes: true

